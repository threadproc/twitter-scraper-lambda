# twitter-scraper-lambda

This is a lambda function that uses Twitter's frontend API to scrape tweets and expose them like v1.1 API objects.

## Usage

`GET /tweet?tweet_ids=<comma-separated list of tweet IDs>`